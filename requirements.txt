# Pinned to versions verified current as of April 2026.
# If anything breaks on install, loosen these to ranges (>=X.Y) one at a time.

# --- Core agent stack ---
langchain==0.3.27
langchain-core==0.3.78
langchain-openai==0.3.39
langchain-anthropic==0.3.27
langgraph==0.6.10
langchain-mcp-adapters==0.2.2

# --- MCP server ---
mcp[cli]==1.18.0

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- UI ---
chainlit==2.11.1

# --- Observability ---
langsmith==0.4.41

# --- Dev / test ---
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
ruff==0.9.0
